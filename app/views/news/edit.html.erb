<% if admin? %>
    <form action="/news/save/<%= @news.id %>" method="post">
      <input type="text" name="title" value="<%= @news.title %>"/>
      <textarea name="content"><%= @news.content %></textarea>
      <input type="submit" value="Сохранить"/>
    </form>
<% end %>